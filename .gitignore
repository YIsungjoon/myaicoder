node_modules/
.next/
dist/
.env
.env.local
.env.*.local
__pycache__/
*.pyc
.venv/
*.egg-info/
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

contech-chatbot/

*.gguf
sungjunCode/

# Central config (contains API keys and tokens)
config/gateway.yaml
config/models.yaml
.env.prod
!config/.gitkeep

# Build outputs
package-lock.json
COMPLETION_SUMMARY_*.md

# bkit session-local state (not shared across machines)
.bkit/agent-state.json
docs/.bkit-memory.json

# bkit agent memory (per-session local cache — each session regenerates)
**/.claude/agent-memory/

# External repos accidentally placed inside project root
# (claudecode = claw-code reference, myaicoder = empty duplicate, portfolio_doc = draft)
/claudecode/
/myaicoder/
/portfolio_doc/